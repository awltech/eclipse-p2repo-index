#if ($localVersion && $localVersion != "")
<!-- Generated by AWLTech p2repo-index-plugin (version $localVersion) on $dateNow -->
#else
<!-- Generated by AWLTech p2repo-index-plugin on $dateNow -->
#end
#if ($version && $version != "")
<!-- Version: $version -->
#end
<head>
	<link rel="stylesheet" type="text/css" href="style.css">
	<title>$repositoryDescriptor.name Update Site</title>
</head>
<body>
	<div id="contents">
		<section>
			<span id="title">You are now on the "$repositoryDescriptor.name" Update Site !</span>
		</section>
		<section>
			<p>
				<div>But as this page is an Eclipse Update Site, it is not intended from browsing...</div>
				<div>To fully use this site, please go into your Eclipse instance, and select the <b>Install New Software</b> option with this URL to access the binaries...</div>
#if ($projectURL && $projectURL != "")
				<div>If you were looking for the documentation of this project, please click <a href="$projectURL">here</a> to be redirected.</div>
#end
			</p>
			<p>
				<span id="subtitle">Below are the available features, proposed by this Update Site:</span>
			</p>
		</section>
		<hr />
		<section>
			<ul>
#foreach ($category in $repositoryDescriptor.categoryDescriptors)
				<li>
					<span id="category">$category.name</span>
					<p>
						<ul>
#foreach ($feature in $category.featureDescriptors)
							<li><span id="feature-name">$feature.name</span> <span id="feature-version">($feature.version)</span></li> <!-- <metadata><identifier>$feature.id</identifier><provider>$feature.provider</provider></metadata> -->
#end
						</ul>
					</p>
				</li>
#end
			</ul>
		</section>
		<hr />
		<section>
			<p>
				<span id="footer">(<a href="https://github.com/awltech/eclipse-p2repo-index">This page has been generated</a> upon an Update Site built on $dateSite.)</span>
			</p>
		</section>
	</div>
</body>